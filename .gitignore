# ── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Data artifacts ──────────────────────────────────────────────────────
# Raw source CSVs (several GB each — never commit)
"Nifty Options Data (Monthly Expiries)/"
"Bank Nifty Options Data (Monthly Expiries)/"
"Nifty Spot.csv"
"Bank Nifty Spot.csv"

# Old-path CSVs (legacy ignore entries)
/Options Data (Monthly Expiries from Jan 2023 - Feb 2026)/
/Spot (Jan 2023 - Feb 2026).csv

# Converted Parquet data (tracked via Git LFS — see below)
backend/data/options_nifty/
backend/data/options_banknifty/
backend/data/spot_nifty.parquet
backend/data/spot_banknifty.parquet

# SQLite history DB (local state, not shared)
backend/data/backtester.db
backend/data/backtester.db-wal
backend/data/backtester.db-shm

# ── Redis runtime snapshot ──────────────────────────────────────────────
*.rdb

# ── Node / Next.js ──────────────────────────────────────────────────────
node_modules/
.next/
out/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Editor / OS ─────────────────────────────────────────────────────────
.vscode/
.idea/
.claude/
.DS_Store
Thumbs.db

# ── Local run artifacts (from start/stop scripts) ───────────────────────
logs/
.run/
